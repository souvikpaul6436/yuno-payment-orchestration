version: '3.8'

services:

  postgres:
    image: postgres

    environment:
      POSTGRES_DB: yuno
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"

  redis:
    image: redis

    ports:
      - "6379:6379"